# Copyright 2022 Robert Carlsen
# See LICENSE file for licensing details.
#
# TEMPLATE-TODO: change this example to suit your needs.
# If you don't need actions, you can remove the file entirely.
# It ties in to the example _on_fortune_action handler in src/charm.py
#
# Learn more about actions at: https://juju.is/docs/sdk/actions

revert:
  description: print/show previous config state
  params:
    snapshot:
      description: "Must be negative indicating the number of steps back in config_changed to revert"
      type: integer
      default: -1
